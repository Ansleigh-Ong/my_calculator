from setuptools import setup, find_packages

setup(
    name="calculator-app",
    version="0.1.0",
    description="Calculator with CI/CD",
    author="Ansleigh",
    packages=find_packages(),
    python_requires=">=3.9",
)